"""
Shared constants used by both coach_availability.py and pipeline.py.

Created 31-Jul-2026 IST (Phase 1 cleanup: this default was previously typed
independently into three separate argparse/def defaults).
"""

DEFAULT_STATUSES = "P,F,N"     # occupied appointment statuses; M (missed) & C (cancelled) excluded
